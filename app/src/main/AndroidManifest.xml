<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myown"
        tools:targetApi="31">
        <activity
            android:name=".Activities.CreateRoutine"
            android:exported="false"
            android:label="CreateRoutine"
            android:theme="@style/Theme.Myown.NoActionBar" />
        <activity
            android:name=".Activities.SelectThing"
            android:exported="false"
            android:label="@string/title_activity_select_thing"
            android:theme="@style/Theme.Myown.NoActionBar" />
        <activity
            android:name=".Activities.SelectEvent"
            android:exported="false"
            android:label="@string/title_activity_select_event"
            android:theme="@style/Theme.Myown.NoActionBar" />

        <activity
            android:name=".Activities.SelectRoutine"
            android:exported="false"
            android:label="@string/title_activity_select_routine"
            android:theme="@style/Theme.Myown.NoActionBar" />
        <activity
            android:name=".Activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Myown.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>